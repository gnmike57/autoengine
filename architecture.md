# Living Database Architecture

This artifact is auto-generated by `npm run build:architecture` to keep Antigravity context perfectly aligned with the SQLite schema.

## Entity Relationship Diagram

```mermaid
erDiagram
    credentials {
        INTEGER id
        TEXT email
        TEXT passwords
        JSON --
        INTEGER next_batch_index
        DATETIME created_at
        TEXT target_sites
    }
    test_runs {
        INTEGER id
        INTEGER credential_id
        TEXT target_site
        TEXT outcome
        TEXT error
        TEXT session_id
        TEXT recording_url
        TEXT final_screenshot_url
        INTEGER batch_index
        TEXT passwords_tried
        JSON --
        TEXT proxy_region
        REAL credits_spent
        DATETIME timestamp
        INTEGER duration_ms
        TEXT ai_verification_status
    }
    imported_files {
        INTEGER id
        TEXT filename
        DATETIME imported_at
    }
    credit_usage {
        INTEGER id
        DATETIME timestamp
        TEXT session_id
        REAL credits_spent
        TEXT credential_email
        TEXT backend
        TEXT outcome
    }
    scheduled_retests {
        INTEGER id
        TEXT email
        DATETIME scheduled_at
        TEXT reason
        DATETIME created_at
        BOOLEAN completed
    }
    email_denylist_db {
        INTEGER id
        TEXT email
        TEXT reason
        DATETIME added_at
        TEXT added_by
        DATETIME expires_at
    }
    progress_snapshots {
        INTEGER id
        DATETIME timestamp
        INTEGER row_index
        TEXT email
        TEXT site
        TEXT outcome
        TEXT session_id
        TEXT data
    }
    evidence_checksums {
        INTEGER id
        TEXT session_id
        TEXT file_path
        TEXT file_type
        TEXT sha256_hash
        DATETIME created_at
    }
    target_winners {
        TEXT target_url
        TEXT best_backend
        DATETIME updated_at
    }
    session_fingerprints {
        TEXT session_id
        TEXT email
        TEXT fingerprint_data
        DATETIME created_at
    }
    credential_status {
        INTEGER credential_id
        TEXT target_site
        TEXT outcome
        TEXT error
        TEXT session_id
        TEXT recording_url
        INTEGER batch_index
        TEXT passwords_tried
        TEXT backend
        TEXT proxy_region
        TEXT ai_verification_status
        DATETIME timestamp
    }
    session_telemetry {
        INTEGER id
        TEXT session_id
        TEXT email
        TEXT target_site
        TEXT backend
        TEXT proxy_key
        TEXT proxy_region
        INTEGER fingerprint_seed
        TEXT ua_hash
        TEXT timing_vector
        JSON --
        JSON --
        JSON --
        TEXT outcome
        REAL block_rate_at_time
        INTEGER attempt_index
        DATETIME timestamp
    }
```


## Schema Definitions

### `credentials`
```sql
CREATE TABLE credentials (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      email TEXT UNIQUE NOT NULL,
      passwords TEXT NOT NULL, -- JSON string array
      password_count INTEGER DEFAULT 0,
      next_batch_index INTEGER DEFAULT 0,
      created_at DATETIME DEFAULT CURRENT_TIMESTAMP
    , target_sites TEXT DEFAULT '["joe","ignition"]')
```

### `test_runs`
```sql
CREATE TABLE test_runs (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      credential_id INTEGER NOT NULL,
      target_site TEXT NOT NULL,
      outcome TEXT NOT NULL,
      error TEXT,
      session_id TEXT,
      recording_url TEXT,
      final_screenshot_url TEXT,
      batch_index INTEGER DEFAULT 0,
      passwords_tried TEXT, -- JSON string array of passwords attempted
      backend TEXT,
      proxy_region TEXT,
      credits_spent REAL DEFAULT 0,
      timestamp DATETIME DEFAULT CURRENT_TIMESTAMP,
      duration_ms INTEGER,
      ai_verification_status TEXT DEFAULT 'not verified',
      FOREIGN KEY(credential_id) REFERENCES credentials(id)
    )
```

### `imported_files`
```sql
CREATE TABLE imported_files (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      filename TEXT UNIQUE NOT NULL,
      imported_at DATETIME DEFAULT CURRENT_TIMESTAMP
    )
```

### `credit_usage`
```sql
CREATE TABLE credit_usage (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      timestamp DATETIME DEFAULT CURRENT_TIMESTAMP,
      session_id TEXT,
      credits_spent REAL,
      credential_email TEXT,
      backend TEXT,
      outcome TEXT
    )
```

### `scheduled_retests`
```sql
CREATE TABLE scheduled_retests (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      email TEXT NOT NULL,
      scheduled_at DATETIME NOT NULL,
      reason TEXT,
      created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
      completed BOOLEAN DEFAULT 0
    )
```

### `email_denylist_db`
```sql
CREATE TABLE email_denylist_db (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      email TEXT UNIQUE NOT NULL,
      reason TEXT,
      added_at DATETIME DEFAULT CURRENT_TIMESTAMP,
      added_by TEXT DEFAULT 'engine',
      expires_at DATETIME
    )
```

### `progress_snapshots`
```sql
CREATE TABLE progress_snapshots (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      timestamp DATETIME DEFAULT CURRENT_TIMESTAMP,
      row_index INTEGER,
      email TEXT,
      site TEXT,
      outcome TEXT,
      session_id TEXT,
      data TEXT
    )
```

### `evidence_checksums`
```sql
CREATE TABLE evidence_checksums (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      session_id TEXT,
      file_path TEXT,
      file_type TEXT,
      sha256_hash TEXT,
      created_at DATETIME DEFAULT CURRENT_TIMESTAMP
    )
```

### `target_winners`
```sql
CREATE TABLE target_winners (
      target_url TEXT PRIMARY KEY,
      best_backend TEXT,
      updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
    )
```

### `session_fingerprints`
```sql
CREATE TABLE session_fingerprints (
      session_id TEXT PRIMARY KEY,
      email TEXT,
      fingerprint_data TEXT,
      created_at DATETIME DEFAULT CURRENT_TIMESTAMP
    )
```

### `credential_status`
```sql
CREATE TABLE credential_status (
      credential_id INTEGER NOT NULL,
      target_site TEXT NOT NULL,
      outcome TEXT NOT NULL,
      error TEXT,
      session_id TEXT,
      recording_url TEXT,
      batch_index INTEGER DEFAULT 0,
      passwords_tried TEXT,
      backend TEXT,
      proxy_region TEXT,
      ai_verification_status TEXT DEFAULT 'not verified',
      timestamp DATETIME DEFAULT CURRENT_TIMESTAMP,
      PRIMARY KEY (credential_id, target_site),
      FOREIGN KEY(credential_id) REFERENCES credentials(id)
    )
```

### `session_telemetry`
```sql
CREATE TABLE session_telemetry (
      id INTEGER PRIMARY KEY AUTOINCREMENT,
      session_id TEXT NOT NULL,
      email TEXT NOT NULL,
      target_site TEXT NOT NULL,
      backend TEXT,
      proxy_key TEXT,
      proxy_region TEXT,
      fingerprint_seed INTEGER,
      ua_hash TEXT,
      timing_vector TEXT,       -- JSON
      network_metrics TEXT,     -- JSON
      dom_metrics TEXT,         -- JSON
      hermes_interventions INTEGER DEFAULT 0,
      outcome TEXT,
      block_rate_at_time REAL,
      attempt_index INTEGER DEFAULT 0,
      timestamp DATETIME DEFAULT CURRENT_TIMESTAMP
    )
```

